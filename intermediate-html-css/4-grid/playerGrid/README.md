# Player Grid

A grid-based layout for displaying items (players/cards) in a responsive grid.

## Features
-   **Grid Layout**: Uses CSS Grid to arrange items.
-   **Responsive**: Adjusts the number of columns based on available width or predefined settings.
